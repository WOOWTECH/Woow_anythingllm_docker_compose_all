apiVersion: v1
kind: Service
metadata:
  name: anythingllm
  namespace: anythingllm
  labels:
    app: anythingllm
spec:
  type: NodePort
  selector:
    app: anythingllm
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: http
      nodePort: 30301
